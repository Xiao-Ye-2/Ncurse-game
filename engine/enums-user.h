#ifndef ENUMS_USER_H
#define ENUMS_USER_H

enum class CollideType {PASS = 0, BOUNCE, STOP, DESTROY, DAMAGE, HEAL, WIN, LOSE};
enum class Action {INVALID = 0, UP, DOWN, LEFT, RIGHT, ALL, SHOOT };

#endif
