#ifndef ENUMS_H
#define ENUMS_H

enum class StatusLine {ZERO = 0, ONE, TWO};
enum GameStatus {CONTINUE, WIN, LOSE};
enum class Color {BLACK = 0, RED, GREEN, YELLOW, BLUE, MAGENTA, CYAN, WHITE};
enum class Direction {UP = 0, DOWN, LEFT, RIGHT, PLAYER};

#endif

